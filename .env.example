PORT=8080

BLUEPRINT_DB_DATABASE=vanilla_api
BLUEPRINT_DB_USERNAME=postgres
BLUEPRINT_DB_PASSWORD=postgres
BLUEPRINT_DB_HOST=localhost
BLUEPRINT_DB_PORT=5432
BLUEPRINT_DB_SCHEMA=public
BLUEPRINT_DB_SSLMODE=disable

# Required for PATCH /api/v1/admin/* endpoints.
ADMIN_API_KEY=replace-with-a-long-random-secret

# The web menu opened by the single Dub QR link through GET /menu.
MENU_APP_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# Create this in Dub workspace Settings > API Keys. Keep it backend-only.
DUB_API_KEY=
# Optional: omit to use the workspace's primary domain (or dub.sh).
DUB_DOMAIN=
DUB_LINK_KEY=menu
